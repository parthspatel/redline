//! Built-in metric registration (populated in later plans).

use super::registry::MetricRegistry;

/// Register all built-in metrics with the given registry.
///
/// This will be populated in plans 04-02 through 04-08.
pub fn register_builtins(_registry: &mut MetricRegistry) {
    // Stub: metrics will be registered by later plans.
}
