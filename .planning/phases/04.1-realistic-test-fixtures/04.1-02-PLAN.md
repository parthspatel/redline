---
phase: 04.1-realistic-test-fixtures
plan: 02
type: execute
wave: 2
depends_on: ["04.1-01"]
files_modified:
  - crates/core/tests/text_processing.rs
  - crates/core/tests/normalizer_tokenizer_matrix.rs
autonomous: true

must_haves:
  truths:
    - "Normalizer tests exercise multi-sentence paragraphs, not just 2-word phrases"
    - "Tokenizer tests verify token counts and spans on realistic text"
    - "Pipeline tests process realistic paragraphs through multi-normalizer chains"
    - "normalizer_tokenizer_matrix.rs imports constants from common module instead of defining its own"
    - "All existing tests still pass (no regressions)"
  artifacts:
    - path: "crates/core/tests/text_processing.rs"
      provides: "Enhanced normalizer/tokenizer/pipeline tests with realistic fixtures"
    - path: "crates/core/tests/normalizer_tokenizer_matrix.rs"
      provides: "Matrix tests using shared constants from common"
  key_links:
    - from: "crates/core/tests/text_processing.rs"
      to: "crates/core/tests/common/mod.rs"
      via: "mod common"
      pattern: "common::"
    - from: "crates/core/tests/normalizer_tokenizer_matrix.rs"
      to: "crates/core/tests/common/mod.rs"
      via: "mod common"
      pattern: "common::multilang::"
---

<objective>
Enhance normalizer, tokenizer, and pipeline integration tests with realistic text fixtures from the shared common module.

Purpose: Replace synthetic 2-word test inputs with multi-sentence paragraphs that exercise realistic text processing scenarios. Move shared constants from normalizer_tokenizer_matrix.rs to the common module.

Output: Enhanced `text_processing.rs` and `normalizer_tokenizer_matrix.rs` with new realistic test cases and shared constant imports.
</objective>

<execution_context>
@/Users/parthpatel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/parthpatel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-realistic-test-fixtures/04.1-RESEARCH.md
@crates/core/tests/common/mod.rs
@crates/core/tests/text_processing.rs
@crates/core/tests/normalizer_tokenizer_matrix.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance text_processing.rs with realistic fixtures</name>
  <files>crates/core/tests/text_processing.rs</files>
  <action>
Add `mod common;` at the top of `text_processing.rs` (after the module doc comment and before `use` statements).

Add the following NEW test functions (do NOT modify or remove any existing tests):

**1. `fn realistic_paragraph_through_full_pipeline()`**
Process `common::TECHNICAL_PROSE` through Lowercase + WhitespaceNormalizer + UnicodeNormalizer -> WordTokenizer.
Assert:
- `result.tokens.len() > 20` (realistic paragraph produces many tokens)
- All tokens resolve via `result.text_store.resolve()`
- `result.normalized == result.normalized.to_lowercase()` (lowercase applied)
- `result.composed_mapping` is Some and `mapping.original_len() == TECHNICAL_PROSE.len()`
- `result.layers.len() == 3` (one per normalizer)

**2. `fn literary_prose_preserves_sentence_structure()`**
Process `common::LITERARY_PROSE` through Lowercase + WhitespaceNormalizer -> WordTokenizer.
Assert:
- Tokens are non-empty
- No double spaces remain in normalized text (`!result.normalized.contains("  ")`)
- All token spans are within original text bounds

**3. `fn multi_paragraph_whitespace_normalization()`**
Process `common::MULTI_PARAGRAPH` through WhitespaceNormalizer -> WordTokenizer.
Assert:
- Multiple tokens produced
- Paragraph separators (`\n\n`) are preserved or handled correctly in normalized text

**4. `fn accented_text_full_pipeline()`**
Process `common::multilang::ACCENTED_FRENCH` through UnicodeNormalizer::default() + RemoveDiacritics + Lowercase -> WordTokenizer.
Assert:
- Result normalized text contains no accented characters (RemoveDiacritics worked)
- All tokens resolve correctly
- CharMapping is valid (original_len matches input length)

**5. `fn simple_prose_char_tokenizer()`**
Process `common::SIMPLE_PROSE` through Lowercase -> CharTokenizer.
Assert:
- Token count equals grapheme count of the normalized text (use `unicode_segmentation::UnicodeSegmentation::graphemes`)
- All spans are within bounds

Do NOT modify any existing test functions. Only add new ones.
  </action>
  <verify>
```bash
cargo test --test text_processing 2>&1 | tail -5
```
All existing and new tests pass.
  </verify>
  <done>
5 new test functions added to text_processing.rs using fixtures from common module. All tests pass including existing ones.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update normalizer_tokenizer_matrix.rs to use common constants</name>
  <files>crates/core/tests/normalizer_tokenizer_matrix.rs</files>
  <action>
Add `mod common;` at the top (after module doc comment, before `use` statements).

**Replace local constants** with imports from common. Change the 7 local constant definitions:
```rust
const ASCII: &str = ...;
const UNICODE: &str = ...;
const CJK: &str = ...;
const EMOJI: &str = ...;
const RTL: &str = ...;
const VIETNAMESE: &str = ...;
const MIXED: &str = ...;
```

to:
```rust
use common::multilang::{
    ASCII, UNICODE_ACCENTED as UNICODE, CJK, EMOJI, RTL, VIETNAMESE, MIXED_SCRIPT as MIXED,
};
```

This keeps existing test code working without changing function bodies (same constant names via aliases).

**Add new test functions** using realistic paragraph fixtures:

**1. In `word_tokenizer_combos` module: `fn realistic_paragraph_lowercase_whitespace_word()`**
Call `assert_pipeline_valid(common::TECHNICAL_PROSE, vec![Box::new(Lowercase), Box::new(WhitespaceNormalizer)], Box::new(WordTokenizer))`.

**2. In `sentence_tokenizer_combos` module: `fn realistic_paragraph_sentence()`**
Call `assert_sentence_pipeline_valid(common::LITERARY_PROSE, vec![Box::new(Lowercase)])`.

**3. In `unicode_combos` module: `fn chinese_paragraph_word()`**
Call `assert_pipeline_valid(common::multilang::CHINESE_PARAGRAPH, vec![Box::new(Lowercase)], Box::new(WordTokenizer))`.

**4. In `unicode_combos` module: `fn french_accented_remove_diacritics_word()`**
Call `assert_pipeline_valid(common::multilang::ACCENTED_FRENCH, vec![Box::new(RemoveDiacritics), Box::new(Lowercase)], Box::new(WordTokenizer))`.

Do NOT modify any existing test function bodies. Only replace the constant definitions and add new tests.
  </action>
  <verify>
```bash
cargo test --test normalizer_tokenizer_matrix 2>&1 | tail -5
```
All existing and new tests pass.
  </verify>
  <done>
Local constants replaced with common module imports. 4 new test functions added using realistic paragraph fixtures. All tests pass.
  </done>
</task>

</tasks>

<verification>
1. `cargo test --test text_processing` — all tests pass (existing + 5 new)
2. `cargo test --test normalizer_tokenizer_matrix` — all tests pass (existing + 4 new, using shared constants)
3. No local constant definitions remain in normalizer_tokenizer_matrix.rs (moved to common)
4. Both files contain `mod common;`
</verification>

<success_criteria>
- text_processing.rs has 5 new tests using common fixtures, all passing
- normalizer_tokenizer_matrix.rs uses shared constants from common module
- normalizer_tokenizer_matrix.rs has 4 new realistic paragraph tests
- Zero regressions: all pre-existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-realistic-test-fixtures/04.1-02-SUMMARY.md`
</output>
