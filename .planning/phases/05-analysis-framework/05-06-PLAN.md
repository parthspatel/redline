---
phase: 05-analysis-framework
plan: 06
type: execute
wave: 4
depends_on: ["05-01", "05-02", "05-03", "05-04", "05-05"]
files_modified:
  - crates/core/tests/analysis_integration.rs
  - crates/core/src/analysis/mod.rs
autonomous: true

must_haves:
  truths:
    - "Full pipeline: register 4 analyzers, run coordinator on DiffResult, get AnalysisReport with 4 results"
    - "AnalysisReport has typed results accessible via report.semantic(), report.readability(), etc."
    - "Panic isolation: one analyzer panics, remaining 4 still produce results"
    - "Circular dependency registration is rejected with error"
    - "EditClassifier categorizes typo fix as Correction and paragraph insert as Expansion"
  artifacts:
    - path: "crates/core/tests/analysis_integration.rs"
      provides: "End-to-end integration tests for analysis framework covering all 5 success criteria"
      min_lines: 200
  key_links:
    - from: "crates/core/tests/analysis_integration.rs"
      to: "crates/core/src/analysis/coordinator.rs"
      via: "Tests run AnalysisCoordinator.run() end-to-end"
      pattern: "coordinator\\.run"
    - from: "crates/core/tests/analysis_integration.rs"
      to: "crates/core/src/analysis/report.rs"
      via: "Tests access typed results via report.semantic(), report.edit_classifier(), etc."
      pattern: "report\\.semantic\\(\\)|report\\.edit_classifier\\(\\)"
---

<objective>
Create comprehensive integration tests verifying the full analysis pipeline end-to-end, plus final module exports cleanup. Tests cover all 5 Phase 5 success criteria from the roadmap.

Purpose: Validates all Phase 5 success criteria with real DiffResults and MetricsEngine. Catches wiring issues between components that unit tests miss. Ensures the public API is clean and complete.
Output: Integration test file with tests covering all 5 success criteria, plus verified clean public API.
</objective>

<execution_context>
@/Users/parthpatel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/parthpatel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-analysis-framework/05-CONTEXT.md
@.planning/phases/05-analysis-framework/05-RESEARCH.md
@.planning/phases/05-analysis-framework/05-01-SUMMARY.md
@.planning/phases/05-analysis-framework/05-02-SUMMARY.md
@.planning/phases/05-analysis-framework/05-03-SUMMARY.md
@.planning/phases/05-analysis-framework/05-04-SUMMARY.md
@.planning/phases/05-analysis-framework/05-05-SUMMARY.md
@crates/core/src/analysis/mod.rs
@crates/core/src/analysis/coordinator.rs
@crates/core/src/analysis/report.rs
@crates/core/tests/diff_integration.rs
@crates/core/tests/metrics_integration.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integration tests covering all 5 success criteria</name>
  <files>crates/core/tests/analysis_integration.rs</files>
  <action>
    Create `crates/core/tests/analysis_integration.rs`.

    **Imports** -- use the public API paths from `redline_core`. Check existing integration tests (`diff_integration.rs`, `metrics_integration.rs`) for the exact import patterns used in this project for `TextProcessor`, `DiffComputer`, `MetricRegistry`, `MetricsEngine`, `register_builtins`, etc. Match those patterns exactly.

    Also import:
    ```rust
    use redline_core::analysis::{
        AnalysisContext, AnalysisCoordinator, AnalysisError, AnalysisReport,
        AnalyzerDependency, AnalyzerMeta, AnalyzerPlugin,
        PluginRegistry, ExecutionPlanner,
        register_builtin_analyzers,
        IntentCategory,
    };
    use redline_core::analysis::report::AnalyzerStatus;
    ```

    **Test helper: build DiffResult + MetricsEngine from two strings.**
    Follow the patterns from existing integration tests. The function should:
    1. Create a `TextProcessor` (use builder or default -- check existing tests)
    2. Process source and target text into `Arc<ProcessedText>`
    3. Create `DiffComputer` and compute `DiffResult`
    4. Create `MetricRegistry`, call `register_builtins`, wrap in `MetricsEngine`
    5. Return `(DiffResult, MetricsEngine)`

    **Test helper: build coordinator with 4 builtins.**
    ```rust
    fn make_coordinator() -> AnalysisCoordinator {
        let mut registry = PluginRegistry::new();
        register_builtin_analyzers(&mut registry).unwrap();
        AnalysisCoordinator::new(registry)
    }
    ```

    **Test helper structs: PanicAnalyzer, MockAnalyzer.**
    - `PanicAnalyzer`: implements AnalyzerPlugin, id "panicker", no deps, analyze() panics
    - `MockAnalyzer { id, deps }`: configurable id/deps, analyze() returns Ok(Box::new(42i32))

    **SUCCESS CRITERIA #1 -- Topological ordering:**
    Register c (no deps), b (dep c), a (dep b). ExecutionPlanner::plan() should return ["c", "b", "a"].

    **SUCCESS CRITERIA #2 -- Circular dependency rejection:**
    Register x, y(dep x). Unregister x. Re-register x(dep y) -> CycleDetected error, never panics.

    **SUCCESS CRITERIA #3 -- Full pipeline with 4 builtins:**
    Build DiffResult from two sentences, run coordinator. Verify all 4 typed accessors return Some. All 4 metadata entries are Success.

    **SUCCESS CRITERIA #4 -- Panic isolation:**
    Register PanicAnalyzer + 4 builtins. Run coordinator. PanicAnalyzer -> Failed with "panic:", all 4 builtins still succeed.

    **SUCCESS CRITERIA #5 -- EditClassifier categorization:**
    - Typo fix: "I saw teh cat." -> "I saw the cat." -- top category should be Correction
    - Paragraph insert: "The cat sat." -> "The cat sat. A new paragraph..." -- top category should be Expansion

    **Additional integration tests:**
    - Semantic result has valid similarity scores [0.0, 1.0]
    - Readability result has non-zero scores
    - Report metadata completeness (4 entries, order_index 0..3)
    - run_filtered subset (only 2 of 4 analyzers)
    - EditClassifier returns all 7 categories per operation, sorted, sum ~1.0
  </action>
  <verify>
    `cd /Users/parthpatel/Documents/projects/redline && cargo test -p redline-core --test analysis_integration 2>&1 | tail -30`
    All integration tests pass.
    `cd /Users/parthpatel/Documents/projects/redline && cargo test -p redline-core 2>&1 | tail -10`
    All tests pass (no regressions from prior phases).
  </verify>
  <done>
    All 5 Phase 5 success criteria verified via dedicated integration tests.
  </done>
</task>

<task type="auto">
  <name>Task 2: Final verification and module exports cleanup</name>
  <files>
    crates/core/src/analysis/mod.rs
  </files>
  <action>
    1. Verify `crates/core/src/analysis/mod.rs` has complete re-exports.

    2. Run full test suite:
       ```bash
       cd /Users/parthpatel/Documents/projects/redline && cargo test -p redline-core 2>&1 | tail -20
       ```
       Verify ALL tests pass (should be 855+ existing + new analysis tests).

    3. Run clippy:
       ```bash
       cd /Users/parthpatel/Documents/projects/redline && cargo clippy -p redline-core -- -D warnings 2>&1 | tail -30
       ```
       Fix any clippy warnings in the analysis module.

    4. If any compile errors, clippy issues, or test failures, fix them. This task is the final gate.
  </action>
  <verify>
    `cd /Users/parthpatel/Documents/projects/redline && cargo test -p redline-core 2>&1 | tail -5`
    `cd /Users/parthpatel/Documents/projects/redline && cargo clippy -p redline-core -- -D warnings 2>&1 | tail -10`
    Both pass cleanly. Zero warnings, zero failures.
  </verify>
  <done>
    Analysis module has clean public API with all types exported. Full test suite passes. No clippy warnings. Phase 5 complete.
  </done>
</task>

</tasks>

<verification>
- `cargo test -p redline-core --test analysis_integration` -- all integration tests pass
- `cargo test -p redline-core` -- ALL tests pass (855+ existing + new)
- `cargo clippy -p redline-core -- -D warnings` -- clean, zero warnings
- All 5 Phase 5 success criteria verified via named tests (sc1 through sc5)
</verification>

<success_criteria>
- All 5 success criteria from roadmap verified via dedicated integration tests
- Full test suite passes with no regressions
- Clean public API with all types properly exported
- No clippy warnings
- EditClassifier returns all 7 categories per operation (locked decision verified)
</success_criteria>

<output>
After completion, create `.planning/phases/05-analysis-framework/05-06-SUMMARY.md`
</output>
